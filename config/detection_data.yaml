# Detection dataset — person + sink + soap_dispenser
#
# Generated by: python scripts/prepare_detection_data.py
# Update 'path' after running that script, or let the script overwrite this file.
#
# Classes match config/config.yaml detection.classes exactly:
#   0 = person         (source: COCO 2017)
#   1 = sink           (source: Open Images V7 — "Sink")
#   2 = soap_dispenser (source: Open Images V7 — "Soap dispenser")

path: data/detection_dataset   # root; overwritten by prepare_detection_data.py
train: images/train
val:   images/val

nc: 3
names:
  0: person
  1: sink
  2: soap_dispenser
